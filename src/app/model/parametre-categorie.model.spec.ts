import { ParametreCategorie } from './parametre-categorie.model';

describe('ParametreCategorie', () => {
  it('should create an instance', () => {
    expect(new ParametreCategorie()).toBeTruthy();
  });
});
